[package]
name = "codescode"
version = "0.1.0"
edition = "2021"
authors = ["SidsOnIt"]
description = "Generic code syntax tokenizer by intent"
license = "OHL"

[dependencies]
logos = "0.16.1"
serde = { version = "1.0.229", features = ["derive"], optional = true }

[features]
serde = ["dep:serde"]
